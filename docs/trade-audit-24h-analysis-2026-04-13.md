# 24시간 거래 데이터 분석 리포트

- 작성 기준 시각(KST): `2026-04-13 09:53:32`
- 데이터 소스: `data/trade-audit-2026-04-13-09-53-32.json`
- 원본 커버리지(UTC): `2026-04-11, 2026-04-12, 2026-04-13` (총 3개 일자)
- 해석 주의: 원본이 3일 누적 요약이므로, 아래 24시간 수치는 **3일 평균 환산값**입니다.

## 1) 핵심 지표 (숫자로 보는 현재 상태)

| 지표 | 값 | 해석 |
|---|---:|---|
| 총 이벤트 | 7,612건 | 시스템은 정상적으로 주기 실행 중 |
| `schedule_probe` | 7,590건 (99.711%) | 대부분이 탐색/진단 이벤트 |
| `guard_block` | 22건 (0.289%) | 진입 직전 가드에서 일부 차단 |
| `entry`/`exit`/`funding` | 모두 0건 | **실거래(시뮬레이션 포함) 전환 0%** |
| 실현 Funding/PnL | 모두 0 USD | 거래 미실행 상태와 일치 |
| `n/a` 라우트 이벤트 | 2,508건 (32.95%) | 라우트 미식별 로그 비중 과다 |
| 최근 probe 간격 | 평균 61.32초 | 분 단위 재평가 루프 동작 중 |

### 24시간 환산(3일 평균)

- 총 이벤트: **2,537.33건/24h**
- `schedule_probe`: **2,530.00건/24h**
- `guard_block`: **7.33건/24h**

## 2) 라우트/실패 패턴

### 상위 라우트(이벤트 비중)

1. `n/a`: 2,508건 (32.95%)
2. `RAVE:binance->bybit`: 520건 (6.83%)
3. `XCN:binance->bybit`: 471건 (6.19%)
4. `CL:bybit->binance`: 431건 (5.66%)

### guard_block 집중 라우트

- `SIREN:binance->bybit`: 5건 (guard의 22.73%, 라우트 블록률 2.38%)
- `RAVE:binance->bybit`: 5건 (guard의 22.73%, 라우트 블록률 0.96%)
- `NOM:binance->bybit`: 3건 (guard의 13.64%, 라우트 블록률 2.00%)

=> 소수 라우트에 guard가 집중되는 편향이 있음.

## 3) 스케줄 상태 기반 원인 분석

`scheduleProbeStates` 10건 기준:

- 상태 분포: `failed` 3건, `canceled` 5건, `scheduled` 2건, `executed` 0건
- 실패/취소 사유:
  - `schedule_replanned`: 5건 (62.5%)
  - `funding_window_shifted`: 3건 (37.5%)
- 슬리피지 관측치: 평균 0.3511%, 최대 2.0882%

즉, 현재 병목은 크게 2가지입니다.

1. **재스케줄링 과다(`schedule_replanned`)**로 진입 직전 취소 빈도가 높음
2. **펀딩 타임 윈도우 이탈(`funding_window_shifted`)**로 실행 직전 실패 발생

## 4) 문제점 정리 (우선순위)

### P0. 거래 전환율 0%

- 근거: `entry/exit/funding=0`, `simPositions=0`, `simTotalFundingEarned=0`
- 영향: 전략의 수익 창출 루프가 사실상 정지

### P1. 재계획(churn)로 인한 기회 손실

- 근거: probe 상태의 50%가 `canceled`, 사유 대부분 `schedule_replanned`
- 영향: 진입 직전까지 간 케이스가 실행으로 이어지지 않음

### P1. 펀딩 타이밍 민감도로 실행 실패

- 근거: 실패 사유 중 `funding_window_shifted` 비중 37.5%
- 영향: 유효 기회라도 타임 윈도우 판정으로 탈락

### P2. 관측 데이터 가독성 저하

- 근거: `n/a` 라우트 32.95%
- 영향: 어떤 자산/거래소 조합에서 병목이 생기는지 파악 난이도 상승

## 5) 해결 방안 (실행형)

### 1) 전환율 복구를 위한 실행 KPI 가드 도입 (즉시)

- 목표: `entry 전환율`을 0% -> 최소 1% 이상으로 복구
- 방식:
  - 15분/1시간 단위로 `probe 대비 entry 전환율` 계산
  - 전환율이 임계치 미만이면 알림 + 스케줄 파라미터 자동 완화(또는 운영자 확인)
- 기대효과: “돌아가지만 체결 안 되는 상태” 조기 탐지

### 2) 재스케줄링 완화 (우선)

- 목표: `schedule_replanned` 비중 62.5% -> 30% 이하
- 방식:
  - 진입 임박 구간에서 재계획 억제 윈도우 확대
  - EV/스프레드가 소폭 변동일 때 기존 스케줄 유지(히스테리시스)
- 기대효과: 취소 감소, 실제 체결 기회 증가

### 3) 펀딩 윈도우 판정 보정 (우선)

- 목표: `funding_window_shifted` 실패율 감소
- 방식:
  - 거래소별 최근 shift 통계를 반영한 동적 허용치 적용
  - 타깃 시점 직전 재검증 횟수/타이밍 조정
- 기대효과: 타이밍 노이즈로 인한 불필요한 차단 감소

### 4) 라우트 품질 기반 선별 강화 (중기)

- 목표: 실패 집중 라우트의 재시도 비용 축소
- 방식:
  - 라우트별 `guard_block율`, `실행성공률`, `평균 슬리피지` 점수화
  - 일정 기간 저품질 라우트 가중치 하향 또는 일시 제외
- 기대효과: 동일 probe 수 대비 실행 효율 개선

### 5) 분석 로그 표준화 (중기)

- 목표: `n/a` 비중 축소, 원인 추적 시간 단축
- 방식:
  - 분석성 `schedule_probe`에도 `opportunityId/probeId/baseAsset`를 일관 기록
  - 대시보드에서 `분석용 probe`와 `실행용 probe` 분리 집계
- 기대효과: 운영자가 “왜 막혔는지”를 즉시 식별 가능

## 6) 단기 목표치 제안 (다음 24시간)

- `entry 전환율`: **>= 1.0%**
- `schedule_replanned` 비중: **<= 30%**
- `funding_window_shifted` 비중: **<= 15%**
- `n/a` 라우트 비중: **<= 10%**

위 목표치 도달 여부만 매일 확인해도, 현재 병목이 해소되는지 빠르게 판단할 수 있습니다.
